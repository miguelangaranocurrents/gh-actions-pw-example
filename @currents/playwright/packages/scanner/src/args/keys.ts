export const keys = {
  config: "--config",
  grep: "--grep",
  grepInvert: "--grep-invert",
  project: "--project",
  inspect: "--inspect",
  outFile: "--out-file",
  c: "-c",
  g: "-g",
} as const;
